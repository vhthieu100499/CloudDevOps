---
- name: extract the zipped artifact
  become: yes
  unarchive:
    src: ~/project/artifact.tar.gz
    dest: .
    
- name: start the app
  become: yes
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start